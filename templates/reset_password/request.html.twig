{% extends 'base.html.twig' %}

{% block title %}Demander un nouveau mot de passe{% endblock %}

{% block body %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <section class="container tuto-container">
        <h1>Demander un nouveau mot de passe</h1>
        <div class="row m-2">
            {{ form_start(requestForm) }}
                {{ form_row(requestForm.email) }}
                <div>
                    <small>
                        Renseignez votre email et nous vous enverrons un lien pour réinitialiser votre mot de passe.
                    </small>
                </div>

                <button class="btn tutodeco-btn">Envoyer ma demande</button>
            {{ form_end(requestForm) }}
        </div>
    </section>


{% endblock %}