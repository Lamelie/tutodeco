{% extends 'base.html.twig' %}

{% block title %}Modifier le tutoriel{% endblock %}

{% block body %}
<section class="container card-border">
    <div class="row align-content-center card">
        <h1 class="login-container-h1 w-100">Modifier le tutoriel</h1>
        <div class="p-2">
            {{ include('tutorial/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <a href="{{ path('tutorial_index') }}">Revenir à la liste</a>

            {{ include('tutorial/_delete_form.html.twig') }}
        </div>
    </div>
</section>
{% endblock %}
